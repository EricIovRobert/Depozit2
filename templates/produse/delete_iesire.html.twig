{% extends 'base.html.twig' %}

{% block title %}Șterge Iesire{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center text-danger">Șterge Iesire</h1>
    <p class="text-center">Ești sigur că vrei să ștergi i: <strong>{{ produs.nume }}</strong>?</p>

    <form method="post" action="{{ path('app_delete_iesire', {'id': produs.id}) }}">
        <div class="text-center">
            <button type="submit" class="btn btn-danger">Confirmă</button>
            <a href="{{ path('app_produse') }}" class="btn btn-secondary">Anulează</a>
        </div>
    </form>
</div>
{% endblock %}
